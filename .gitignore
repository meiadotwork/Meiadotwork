node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Working data — images, manifests and generated tags stay out of git.
# public/designs.json is committed with placeholder data so the site builds
# from a clean checkout; a real run overwrites it locally.
/data/
/public/thumbs/
.env
.env.local


